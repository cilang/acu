{
  "scope": "Computing: Apply topological, homotopical, and Hamiltonian operations to one or more Factors to produce new Factors, resonance alignments, linked ensembles, or evolved state vectors.",
  "name": "Interaction Protocol",
  "label": "Interaction",
  "role": "To compute transformations, combinations, and resonances among Factors, treating them as mathematical objects in the ACU type theory with symplectic dynamics.",
  "definition": "Interaction is the computing layer of the ACU engine. It provides six core operations: (1) Resonance computation – a scalar measure of structural alignment between two Factors; (2) Combination – merging Factors via superposition, tensor product, or Reidemeister cancellation; (3) Transformation – applying Reidemeister moves as homotopy rewrites and canonical transformations to a single Factor; (4) Narrative transition – evolving a Factor’s state vector along Hamiltonian flow with transport of manifestations; (5) Embedding interaction – computing linking numbers, braid group actions, and inter‑Factor Hamiltonians using the SO(4) placement coordinates and framing; (6) Probability matrix usage – sampling active edges from the Usepong stratum’s adjacency matrices. Every operation respects phase closure, state conservation, and, where applicable, the total link invariant of the embedded knots. The protocol treats Factors as terms of a dependent sum type over the knot complement; interactions are proof‑relevant transformations that preserve the topological invariants dictated by Ariadi.",
  "manifestation": "Two Factors – ‘MysticSociety’ (binary stratum, n=2,m=4, placement u=1,v=1, framing 0) and ‘CyberSociety’ (paired stratum, n=1,m=3, placement u=i,v=j, framing 1) – interact. Resonance is computed (R = 0.68, ‘Harmonic’). They are combined via tensor product into a meta‑Factor with doubled Cilang nodes (Mystic⊗Cyber). The resulting Hamiltonian includes a new inter‑Factor coupling term J_link derived from the Gauss linking integral of their embedded knots (Lk = 3). A Reidemeister‑3 move braids three nodes of MysticSociety, adjusting its internal phase relations while preserving its invariants. The entire ensemble’s state is then advanced three narrative steps, transporting all manifestations and checking that the total phase closure remains zero.",
  "operations": {
    "resonance_computation": {
      "description": "Compute a resonance value between two Factors A and B, indicating their degree of homotopy‑theoretic and Hamiltonian alignment. Higher resonance means more equivalent structure (up to univalence) and closer phase synchronisation.",
      "inputs": { "factor_A": "Factor (complete)", "factor_B": "Factor (complete)" },
      "process": [
        "1. Retrieve both Factors’ Cilang type definitions and term manifestations for all 19 nodes.",
        "2. For each node, compute a type‑equivalence strength:",
        "   – if definitions are syntactically identical → contribution 1.0.",
        "   – else attempt to prove IsEquiv(definition_A[n] ≃ definition_B[n]); if a witness exists → contribution 0.8 (univalent equivalence).",
        "   – otherwise → contribution 0.2.",
        "   Normalise the sum to obtain a base component R_types ∈ [0,1].",
        "3. For nodes with equivalent types, compare the inhabiting terms (manifestations):",
        "   – if an explicit path (transport/equality) links manifestation_A to manifestation_B, add +0.1 per node (max +0.9). This yields R_terms.",
        "4. Factor in resonance depth alignment: Δn = |n_A − n_B|, Δm = |m_A − m_B|. Penalty = (Δn + Δm) / (max(n_A,n_B) + max(m_A,m_B) + 1).",
        "5. Stratum compatibility: if both Factors use the same stratum (or compatible strata per the global aggregate), multiply by 1.0; else multiply by 0.5 (S).",
        "6. Phase alignment: φ_align = cos(phase_A − phase_B), mapped to [0,1] via (φ_align+1)/2.",
        "7. Overall resonance R = clamp( (0.4·R_types + 0.3·R_terms + 0.3·φ_align) × S × (1 − depth_penalty) , 0, 1 ).",
        "8. Return R and a qualitative interpretation:",
        "   – R > 0.8 → ‘Univalent’ (isomorphic structures)",
        "   – 0.5 ≤ R ≤ 0.8 → ‘Harmonic’ (strong overlap)",
        "   – 0.3 ≤ R < 0.5 → ‘Dissonant’ (partial alignment)",
        "   – R < 0.3 → ‘Orthogonal’ (almost no shared structure)."
      ],
      "outputs": {
        "resonance_value": "number ∈ [0,1]",
        "interpretation": "string",
        "type_equivalences": "list of node‑wise equivalences and any univalence witnesses found"
      }
    },
    "combination": {
      "description": "Combine two Factors into a new Factor representing their superposition, tensor product, or Reidemeister‑2‑merge. Uses HoTT type‑theoretic constructions, merges Cilang assignments, and automatically simplifies via univalence and Hamiltonian cancellation. When combined, the Factors' embedded knots (via coordinates) are also merged in S³, producing a new placement and a combined link. ",
      "inputs": {
        "factor_A": "Factor",
        "factor_B": "Factor",
        "combine_mode": {
          "type": "string",
          "enum": ["superposition", "tensor_product", "reidemeister_merge"],
          "default": "superposition"
        },
        "new_namespace": "string (optional; default = A.namespace + '_' + B.namespace)"
      },
      "process": {
        "superposition": [
          "Create new namespace.",
          "For each Cilang node n, define the new node type as a record merge of A.def[n] and B.def[n] (Σ‑type or union depending on common structure).",
          "Apply univalence: if A.def[n] ≡ B.def[n], collapse to a single field; else keep both.",
          "Combine manifestations: if types identical, pair terms (manifest_A[n], manifest_B[n]); if equivalent, transport one to the other.",
          "Combine resonance: n_new = max(n_A, n_B), m_new = max(m_A, m_B), chirality = dominant (or amphichiral if opposite).",
          "Combine states: value_new[n] = (value_A[n] + value_B[n]) mod 3; phase_offset_new[n] = (phase_A[n] + phase_B[n]) mod 2π.",
          "Stratum: if both use same stratum, keep it; else default to 'global'.",
          "Hamiltonian terms: add the two internal Hamiltonians plus a new inter‑Factor coupling derived from the relative coordinate placement (see embedding interaction).",
          "Coordinates placement: set to the product of the two SO(4) rotations (u_new = u_A · u_B, v_new = v_A · v_B) and sum of framings modulo 2."
        ],
        "tensor_product": [
          "Treat A and B as two independent subsystems (components of a link).",
          "Create a Factor whose Cilang assignment is the disjoint union of two 19‑node blocks (38 nodes in the internal representation, but still mapped to the 19 canonical crossings by duplicating the scaffold – this may require an internal 'double‑cover' mode stored in context).",
          "For nodes that appear in both, define a product type (A.def × B.def) and pair the terms.",
          "Resonance: n_new = n_A + n_B, m_new = m_A + m_B.",
          "Stratum: 'global' (or a new 'tensor' stratum if defined).",
          "View_mode: set to 'link' with two components; each component retains its original closure conditions.",
          "Hamiltonian: sum of individual Hamiltonians, with inter‑component coupling terms computed from the Gauss linking integral of the two knots in S³ (using their SO(4) placements).",
          "Coordinates: store both original placements and the combined link diagram."
        ],
        "reidemeister_merge": [
          "Apply a Reidemeister‑2 like simplification between the two Factors, treating them as sub‑diagrams of a larger knot/link.",
          "Identify pairs of nodes (u from A, v from B) with opposite states and edge connections that allow a canceling homotopy.",
          "Remove those two nodes from the combined Cilang assignment (set their definitions to unit type and manifestation to trivial).",
          "Reduce m by the number of canceled path constructors.",
          "Adjust Hamiltonian by removing the corresponding coupling terms.",
          "The resulting Factor is a single term, often simpler than either parent."
        ]
      },
      "outputs": {
        "combined_factor": "A new complete Factor (or link‑mode Factor with two components for tensor_product).",
        "simplification_applied": "list of univalence merges, Reidemeister cancellations, or link invariants computed"
      }
    },
    "transformation": {
      "description": "Apply a single Reidemeister move (R1, R2, R3) to a Factor, reinterpreted as a homotopy rewrite on the Cilang HIT and, simultaneously, a canonical transformation on its Hamiltonian phase space. These operations preserve the T(19,2) knot type and all ACU invariants.",
      "inputs": {
        "factor": "Factor",
        "move": "string (R1, R2, R3)",
        "location": "object identifying which nodes/edges are involved (optional; system can auto‑select based on stratum topology)"
      },
      "process": {
        "R1": {
          "description": "Twist insertion or removal (self‑loop). Adds or removes a full 2π phase cycle.",
          "effect": "Select a node u. In the HIT type of u, add a new path constructor `loop_u : u = u` (if inserting) and increase its phase_offset by 2π (π in units of π? Using our convention, phase_offset is in π units, so 2π → +2 to offset). If removing, delete the loop and decrease phase_offset by 2. Transport the term across the loop using the J‑rule. The Hamiltonian gains or loses a self‑coupling term J_uu sin(0) which is zero, so no net change.",
          "constraint": "After the move, phase closure must still hold; the total phase change around any cycle must remain 0 mod 2π."
        },
        "R2": {
          "description": "Cancel or create a pair of oppositely‑oriented edges between two nodes.",
          "effect": "Find two distinct nodes u and v with a direct path p: u=v and an inverse path q: v=u whose compositions can be reduced to reflexivity. In the HIT, remove p and q (set them to reflexivity). The corresponding Hamiltonian terms J_uv sin(θ_u−θ_v) and J_vu sin(θ_v−θ_u) cancel because J_uv = J_vu and sin is odd. Adjust the Cilang values accordingly; the two nodes may become trivial (unit type) if no other paths involve them, but the mandatory 19‑cycle prevents full removal of essential nodes.",
          "constraint": "Must not break the mandatory Usepong cycle or disconnect the Factor’s underlying knot diagram."
        },
        "R3": {
          "description": "Three‑way braid relation. Shifts phase relations among three nodes forming a triangle in the stratum.",
          "effect": "Given nodes a,b,c with edges a→b, b→c, c→a (or a cyclic permutation), apply the homotopy (p_ab ∘ p_bc ∘ p_ca) = (p_ac ∘ p_cb ∘ p_ba). This is implemented by adjusting the phase_offsets of the three nodes by ±2π/3 each (mod 2π) and swapping the chirality of the local interaction if needed. The Hamiltonian is updated by permuting the coupling constants accordingly.",
          "constraint": "The triangle must exist in the chosen stratum with non‑zero probability."
        }
      },
      "outputs": {
        "transformed_factor": "Factor after applying the move.",
        "phase_shift_applied": "Total phase change (in π units) applied to the system.",
        "proof_of_invariance": "Witness that all topological invariants (knottedness, phase closure, state conservation) are preserved."
      }
    },
    "narrative_transition": {
      "description": "Evolve a Factor’s Cilang state vector by applying one or more discrete Hamiltonian steps, simultaneously transporting manifestations via the J‑rule. This operation computes the next state(s) of a single Factor, and can be chained to produce a full trajectory for later interpretation or grammar rendering.",
      "inputs": {
        "factor": "Factor (with current state vector)",
        "steps": "integer (≥1, default 1)",
        "chirality_follow": "boolean (default true, uses the Factor’s chirality to determine direction)"
      },
      "process": [
        "For each step:",
        "  1. Determine the transition direction from factor.type_binding.resonance.chirality, or override if chirality_follow=false.",
        "  2. For each explicit node (0‑f) that is 'active' (value ≠ 0 or connected via a Usepong edge to a node that changed in the previous step), apply the ternary transition: right‑handed +1→0→-1→+1; left‑handed -1→0→+1→-1.",
        "  3. Translate the state change into a path p : value_old = value_new in the Ternary HIT. Transport the node’s manifestation along p to obtain new_manifestation.",
        "  4. Update phase_offsets: for nodes involved, shift by ±2π/3 according to chirality. For nodes in a triangle, apply the appropriate ±2π/3 braid shift.",
        "  5. Recompute the global phase closure and state conservation witnesses. If they fail, the step is rejected or automatically compensated by adjusting other node phases (spreading the error).",
        "  6. Append a narrative_step_log entry: step number, nodes changed, old/new values, transported terms, phase shifts.",
        "  7. Optionally, update the Factor’s metadata.updated_at and increment a step counter in context."
      ],
      "outputs": {
        "new_factor_state": "The Factor with updated cilang values, phase_offsets, and manifestations after the specified number of steps.",
        "narrative_step_log": "Array of objects detailing each step’s changes."
      }
    },
    "embedding_interaction": {
      "description": "Compute linking numbers, braid relations, and inter‑Factor Hamiltonian coupling terms using the SO(4) × ℤ placement coordinates of two or more Factors. This operation turns the Factor Library into a true S³ link of framed knots, enabling physically‑grounded interactions between otherwise independent Factors.",
      "inputs": {
        "factors": "array of at least two Factors, each with complete `coordinates.placement` and `coordinates.framing`",
        "computation": "string enum: 'linking_number', 'braid_word', 'inter_hamiltonian' (or 'all')"
      },
      "process": {
        "linking_number": [
          "For each pair of Factors (A,B), retrieve their SO(4) placements R_A = (u_A, v_A), R_B = (u_B, v_B).",
          "Compute the relative isometry R_AB = R_B⁻¹ ∘ R_A, which maps the canonical knot K₀ to the position of K_A relative to K_B.",
          "Using the fixed parametrisation of T(19,2), compute the Gauss linking integral: Lk(A,B) = (1/4π) ∮_K_A ∮_K_B (r_A − r_B)·(dr_A × dr_B) / |r_A − r_B|³.",
          "The result is an integer (framed linking number also depends on framings τ_A, τ_B, contributing an extra twist)."
        ],
        "braid_word": [
          "Project the link of N Factors onto a plane (using a chosen projection direction derived from the quaternion pairs).",
          "Identify the braid group generator σ_i for each crossing between component i and i+1.",
          "Record the word in the braid group B_N representing the current spatial arrangement."
        ],
        "inter_hamiltonian": [
          "For each pair of Factors with a non‑zero linking number, compute a coupling constant J_AB = λ · Lk(A,B), where λ is a global coupling strength (default 1).",
          "Add an inter‑Factor term to the total Hamiltonian of the ensemble: H_inter = Σ_{A<B} J_AB sin(Φ_A − Φ_B), where Φ_A is a global phase for Factor A (e.g., the sum of its internal phases).",
          "This coupling encourages the two stories to synchronise their internal cycles when appropriate, reflecting the physical fact that linked knots influence each other’s twist."
        ]
      },
      "outputs": {
        "linking_matrix": "Matrix of integers Lk_{ij}.",
        "braid_word": "String representing the braid arrangement.",
        "inter_hamiltonian": "Additional Hamiltonian terms that can be appended to the bridge of a combined Factor or used in dynamic interaction."
      },
      "note": "This operation realises the 'ocean of strands' metaphor: each Factor is a ribbon; their SO(4) placement determines how they twist around one another; the linking numbers and braid words are the narrative‑level analogues of physical entanglement."
    },
    "probability_matrix_usage": {
      "description": "Utilise the selected Usepong stratum’s probability matrix to determine which satellite edges exist concretely in a given interaction. This is a stochastic helper used internally by combination, transition, and embedding interaction to decide active edges.",
      "process": [
        "For any relevant pair of Cilang nodes (u,v) that are not part of the mandatory 19‑cycle, look up P(u→v) in the stratum’s probability matrix.",
        "If P > random(0,1), the edge exists; a path constructor `p_uv : u ≡ v` is made available for transitions and combinations. Otherwise, the edge is absent (no beat possible directly).",
        "In the HoTT realisation, this models the probabilistic volume of the corresponding 2‑path in the complement’s moduli space.",
        "In Hamiltonian terms, edges with higher probability have a stronger coupling J_uv, directly influencing scene intensity."
      ]
    }
  }
}
